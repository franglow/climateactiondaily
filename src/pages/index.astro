---
import BaseLayout from "../layouts/BaseLayout.astro";
import BuyButtons from "../components/BuyButtons.astro";
import CoverCard from "../components/CoverCard.astro";

// Placeholders (puedes editar aquí)
const BOOK_TITLE = "Climate Change: A Guide to Everyday Action";
const BOOK_SUBTITLE = "Practical Steps for Sustainable Living";
const SHORT_DESC = "Short description placeholder (2–3 sentences) about the book and its value.";
const LONG_DESC = `What if your personal well-being and the planet’s health were intertwined?
This guide invites us to see ourselves not as visitors, but as parts of the Earth itself. When we shift our perspective to one of interconnection, even the smallest actions take on profound meaning.
By blending practical steps with mindful awareness, this guide weaves environmental care with personal growth and offers a clear and achievable path toward habits rooted in gratitude, clarity, and purpose.
Change begins with acknowledging who we are.
We, ourselves, are Earth.`;
const COVER_URL = "/images/cover.png";
const RETAILERS = [
  { name: "Amazon", url: "#" },
  { name: "Bookshop", url: "#" },
];

const pageTitle = `${BOOK_TITLE} — ${BOOK_SUBTITLE}`;
const pageDesc  = SHORT_DESC;
const ogImage   = "/og-default.jpg";
---

<BaseLayout title={pageTitle} description={pageDesc} ogImage={ogImage} lang="es">
  <section class="py-12 grid md:grid-cols-2 gap-10 items-start">
    <div>
      <h1 class="font-serif text-4xl tracking-tight">{BOOK_TITLE}</h1>
      <p class="mt-3 text-xl text-stone-700"><strong>{BOOK_SUBTITLE}</strong></p>

      <BuyButtons retailers={RETAILERS} />

      <div class="mt-10">
        <div class="flex gap-6 border-b border-stone-300">
          <span class="-mb-px border-b-2 border-stone-900 pb-3">Description</span>
        </div>
        <div class="prose prose-stone max-w-none mt-6">
          {LONG_DESC}
        </div>
      </div>
    </div>

    <div>
      <CoverCard src={COVER_URL} alt="Portada del libro" />
      <div class="mt-6 flex justify-center">
        <a href="#" class="px-5 py-3 rounded-xl bg-stone-900 text-white hover:opacity-90">Comprar</a>
      </div>
    </div>
  </section>
</BaseLayout>